# Backend API URL
# Use /api to route through Vite's dev proxy (avoids CORS issues in development)
VITE_API_URL=/api

# The actual backend URL for the proxy target
VITE_PROXY_TARGET=https://your-backend-api.com
